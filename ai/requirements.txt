# Dataset pipeline (Phase 2.1). Install into ai/.venv:
#   ai/.venv/bin/pip install -r ai/requirements.txt
#
# Versions are pinned exactly: a run is reproducible from (this file, the
# config YAML, the git commit). These are the versions the Phase 2.1 suite is
# verified against on Python 3.12-3.13.
torch==2.13.0
torchvision==0.28.0
numpy==2.5.2
pandas==3.0.5
pillow==12.3.0
pydantic==2.13.4
pyyaml==6.0.3

# Model development (Phase 2.2).
# timm supplies the ImageNet-pretrained EfficientNet-B4 and its head
# replacement; scikit-learn supplies the evaluation metrics (F1, recall,
# AUC-ROC, confusion matrix).
timm==1.0.28
scikit-learn==1.9.0
